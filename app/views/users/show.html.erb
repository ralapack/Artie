<p id="notice"><%= notice %></p>

<img src="<%= @user.spotify_user.images.first.url %>"><img>
<br/>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
<%= link_to 'Sign in with Spotify', '/auth/spotify' %>
